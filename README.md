# example-express-web-api

Example restful api, with swagger and in-memory sqlite db.

## Getting started

```sh
# install dependencies
npm install

# run via node
node ./bin/www

# or run via npm
npm run start
```
